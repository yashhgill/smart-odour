# Superseded

This guide described the earlier Render + Neon architecture and the pre-portal
deployment steps. Both have been replaced.

**Read `SETUP.md` instead.** It covers deployment, build verification, the
custom domain, admin and user account creation, Telegram alerts, and the
hardware wiring and firmware, all against the current build.

Kept only so links to it do not dead-end.
